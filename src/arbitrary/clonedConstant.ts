import { constant } from './constant';

/**
 * For `value`
 * @param value - The value to produce
 * @remarks Since 1.8.0
 * @deprecated Switch to {@link constant} instead
 * @public
 */
export const clonedConstant = constant;
