name: Build Status

on:
  push:
    branches:
      - main
      - 'next-*_*_*'
      - 'fix-v*'
    tags:
      - 'v**'
      - '*/v**'
  pull_request:
    branches:
      - main
      - 'next-*_*_*'
      - 'fix-v*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pre_pub_poc:
    name: 'Publish any'
    outputs:
      tag_count: ${{steps.tag_count.outputs}}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: '6d8ef6d0585534757f48fba6092cd9943f764406'
          fetch-depth: 0
      - id: tag_count
        name: Check if one tag matches the package
        run: git tag --points-at HEAD | wc -l
  pub_poc:
    name: 'Publish ${{matrix.package-name}}'
    runs-on: ubuntu-latest
    needs: pre_pub_poc
    if: needs.pre_pub_poc.outputs.tag_count != '0'
    strategy:
      matrix:
        include:
          - version-prefix: 'v'
            package-name: 'fast-check'
          - version-prefix: 'ava/v'
            package-name: '@fast-check/ava'
          - version-prefix: 'jest/v'
            package-name: '@fast-check/jest'
          - version-prefix: 'packaged/v'
            package-name: '@fast-check/packaged'
          - version-prefix: 'poisoning/v'
            package-name: '@fast-check/poisoning'
          - version-prefix: 'vitest/v'
            package-name: '@fast-check/vitest'
          - version-prefix: 'worker/v'
            package-name: '@fast-check/worker'
    steps:
      - uses: actions/checkout@v3
        with:
          ref: '6d8ef6d0585534757f48fba6092cd9943f764406'
          fetch-depth: 0
      - id: check_has_tag
        name: Check if one tag matches the package
        run: git tag --points-at HEAD | grep '^${{matrix.version-prefix}}[0-9][0-9]*\.[0-9][0-9]*\.[0-9][0-9]*'
        continue-on-error: true
      - if: steps.check_has_tag.outcome == 'success'
        name: Publish the package
        run: echo "Publishing"
  #pub_poc_check:
  #  name: Check publish fast-check
  #  runs-on: ubuntu-latest
  #  outputs:
  #    status: ${{steps.check_has_tag.outcome}}
  #  steps:
  #    - uses: actions/checkout@v3
  #      with:
  #        ref: '6d8ef6d0585534757f48fba6092cd9943f764406'
  #        fetch-depth: 0
  #    - id: check_has_tag
  #      name: Check if one tag matches the package
  #      run: git tag --points-at HEAD | grep '^v[0-9][0-9]*\.[0-9][0-9]*\.[0-9][0-9]*'
  #      continue-on-error: true
  #pub_poc_do:
  #  name: Publish fast-check do
  #  runs-on: ubuntu-latest
  #  needs: pub_poc_check
  #  if: needs.pub_poc_check.outputs.status == 'success'
  #  steps:
  #    - name: Publish the package
  #      run: echo "Publishing"
  #pub_poc_vicheck:
  #  name: Check publish vifast-check
  #  runs-on: ubuntu-latest
  #  outputs:
  #    status: ${{steps.check_has_tag.outcome}}
  #  steps:
  #    - uses: actions/checkout@v3
  #      with:
  #        ref: '6d8ef6d0585534757f48fba6092cd9943f764406'
  #        fetch-depth: 0
  #    - id: check_has_tag
  #      name: Check if one tag matches the package
  #      run: git tag --points-at HEAD | grep '^vitest/v[0-9][0-9]*\.[0-9][0-9]*\.[0-9][0-9]*'
  #      continue-on-error: true
  #pub_poc_vido:
  #  name: Publish vifast-check do
  #  runs-on: ubuntu-latest
  #  needs: pub_poc_vicheck
  #  if: needs.pub_poc_vicheck.outputs.status == 'success'
  #  steps:
  #    - name: Publish the package
  #      run: echo "Publishing"
